{
  "name": "expenseflow",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd client && bun run dev",
    "build": "cd client && bun install && bun run build",
    "install:all": "cd client && bun install",
    "test:rules": "firebase emulators:exec 'vitest run tests/firestore.rules.test.js'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "vitest": "^4.1.10"
  }
}
